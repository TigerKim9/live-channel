import React from 'react';
import { Typography, Box } from '@mui/material';

const StreamDetail: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Stream Detail</Typography>
    </Box>
  );
};

export default StreamDetail;
